# Skill 变更记录（CHANGELOG）

本文件记录 `exam-paper-analyzer` 这个 Skill 在**每次实际使用过程中对自身**
（结构 / 功能 / 脚本 / 参考文档 / SKILL.md）所做的修改。

## 为什么需要这份记录

- 后期优化 Skill 时，能快速知道「哪些部分被动过、为什么动」，避免盲目改动主体结构导致功能损坏。
- 维护记录仅保留在本文件，不写入最终的分析成果（分析报告只面向用户交付试卷分析内容）。

## 记录格式（每次修改追加一条）

```text
### YYYY-MM-DD
- **改动位置**：<文件 / 章节 / 脚本名>
- **改动内容**：<做了什么>
- **改动原因**：<为什么>
```

> 追加新条目时请放在「变更历史」下方的最新位置（即紧跟在本说明之后），保持从新到旧排列。

## 变更历史

### 2026-09-06
- **改动位置**：`scripts/validate_question_bank.py`
- **改动内容**：选择题答案字母校验跳过 `answer_source: "未提供（待补答案）"` 的快速入库题，仍保留选项数量、字段、图片和 LaTeX 污染校验
- **改动原因**：导入 `02_9月月考_标准中间题目包.zip` 时，原包无答案，快速入库题答案为 `待教师确认`，校验器误按单选题要求必须存在 A-D 答案字母导致 10 个假错误

### 2026-09-06
- **改动位置**：新增 `scripts/import_standard_package.py`；更新 `SKILL.md` 第 3.4 步和第 3.5 步答案来源规则；更新 `scripts/validate_question_bank.py`
- **改动内容**：① 新增标准中间题目包快速入库脚本，统一完成包体检、LaTeX 控制字符污染拦截、分库定位、图片/原题截图复制、全局 ID 分配、`_papers.json` / `_meta.json` / `_index.md` 更新；② 脚本支持 `--latest-download` 自动读取下载目录最新 zip，规避 Windows PowerShell 中文路径/中文参数 GBK 乱码；③ 对无答案标准题包支持先快速入库，标记 `answer_source: "未提供（待补答案）"`，后续再批量补答案解析，避免录入被逐题解答拖慢；④ 校验器新增该答案来源枚举
- **改动原因**：本次导入 `01_第一单元测试_标准中间题目包.zip` 时，耗时主要来自中文路径编码、临时脚本、包体检、配图复制和即时补答案；将重复流程固化为可复用脚本，便于后续用户持续提供标准题包快速入库

### 2026-09-06
- **改动位置**：`scripts/latex2omml.py`、`scripts/render_docx_practice.py`、`scripts/validate_question_bank.py`、`SKILL.md` 第 3.4 步
- **改动内容**：① `latex2omml.py` 支持 `^\circ` 这类无花括号命令作为上下标原子，避免 Word 中角度显示成 `circ`/乱码；② `render_docx_practice.py` 新增 `normalize_question_text()`，统一清洗题库/中间题包中被错误转义的 LaTeX 控制字符（如 `\frac`→FF+`rac`、`\angle`→BEL+`ngle`、`\text`→TAB+`ext`、`\neq`→换行残片）；③ `validate_question_bank.py` 增加公式控制字符体检，入库后可提前拦截此类坏数据；④ `SKILL.md` 补充中间题包 LaTeX 反斜杠保护规则
- **改动原因**：本次为吴天豪生成数学练习时，发现中间题包/题库 JSON 中公式反斜杠被转义破坏，导致 Markdown/Word 出现 `rac`、`ngle`、`circ` 等乱码；需把临时修复固化为导入、校验、导出的长期补丁

### 2026-09-06
- **改动位置**：`SKILL.md` 适用场景与新增「第 3.4 步：标准中间题目包导入（AI-1 → AI-2 分层）」；题库 `question-bank/初三/数学/`
- **改动内容**：① 将用户提出的“原始试卷 → AI-1 数字化/转换层 → 标准中间题目包 → AI-2 题库理解/整理层 → 正式题库”流程固化进 Skill；② 明确中间题包导入前需体检 `package_manifest.json`、各卷 `manifest.json/questions.json` 和转换置信度/风险字段；③ 正式题库记录保留 `raw_item_id`、`source_file`、`score`、`original_image`、`conversion_confidence`、`conversion_requires_review`、`conversion_issues`；④ 导入桌面 `两份试卷_标准中间题目包.zip` 中 2 份初三数学试卷共 46 题，编号 Q-00434~Q-00479，并复制配图与原题截图；⑤ 修复初三数学存量图片裸路径为分库相对路径，最终题库体检 0 错误、1 警告（46 题 AI 解答待教师确认）
- **改动原因**：用户希望把试卷数字化与题库理解分层，由外部先生成标准中间题包，再交由本 Skill 进行教学理解、标签规范化、答案解析和正式入库

### 2026-09-05
- **改动位置**：新建分库 `question-bank/初三/数学/`（首个数学库）；录入 2026 广东中考数学选择题 1-10（Q-00323~Q-00332）；SKILL.md 第一步新增「Word 公式格式检测」避坑规则
- **改动内容**：① 录入 2026 年广东省中考数学试卷（初三/数学，中考，2026-06-30）首批 10 道选择题，全部视觉识别转录公式为 LaTeX，answer_source=真题答案；② 发现**数学教辅 docx 公式为 MathType OLE 对象**（163 个 oleObject.bin，document.xml 中 oMath=0）——extract_docx 提取会丢失全部公式，改走 PDF 渲染视觉识别路线并沉淀为 SKILL.md 规则；③ 剩余 13 题（填空 11-15、解答 16-23）公式密集，分批续录；④ 录入中曾误读第 10 题"连接 B'C'"为"B'C"，放大局部图核实后修正——多题干图形细节存疑时应放大重看，不臆测
- **改动原因**：用户要求录入 2026 广东中考试卷（吴天豪学生资料，初三数学）

### 2026-09-05
- **改动位置**：题库架构重构为**分库**（按年级/科目）；根级新增 `_meta.json`（全局 id 计数器+注册表）；`validate_question_bank.py` 重写为多库遍历版；`question-bank/README.md` 重写；两个 skill 的 SKILL.md 路径规则同步
- **改动内容**：`question-bank/` 由单库改为 `{年级}/{科目}/` 分库（现有 322 题迁入 `高三/物理/`，含 images/ 21 个试卷目录）。**id 保持全局唯一**——由根级 `_meta.json` 的 `next_id` 统一分配（当前 323），跨库绝不重复。体检脚本升级：遍历所有分库 + 跨库 id 唯一性 + 归属一致性（库内题的 grade/subject 必须与所在分库匹配）+ `_meta.json` next_id 校验。迁移后体检 0 错误
- **改动原因**：用户带不同年级、不同科目的学生，单库混合存放易混乱；分库后按学生年级+科目直接定位题库。id 保持全局是为了出题引用无歧义（避免两个库都出现 Q-00123）

### 2026-09-05
- **改动位置**：题库新增 `answer_source` 字段（322 题存量标注）；`SKILL.md` 第 3.5 步新增「答案来源标注」规则；`validate_question_bank.py` 枚举校验 + AI 待确认统计
- **改动内容**：新增 `answer_source` 枚举字段——`真题答案`（原卷自带）/ `AI解答（待教师确认）`（图片无答案时 AI 解答生成）/ `教师确认`（AI 解答经教师核对）。SKILL.md 规定无答案图片录入时 AI 解答必须完成四项自检（解析与选项对照、过程自洽、量纲检查、多选逐项判断），并**如实标注** AI 来源不得与真题答案混淆；体检脚本对 AI 待确认题发出警告提醒教师优先抽查
- **改动原因**：用户询问"图片无答案时如何处理"——暴露出 AI 补答与真题答案无法区分的质量隐患，落地来源标注机制让教师可优先抽查低置信来源的题

### 2026-09-05
- **改动位置**：题库 id 体系升级（322 题全部重编号）；`SKILL.md` id 规则、`validate_question_bank.py` 校验；两份已交付练习的 MD/DOCX 引用同步更新
- **改动内容**：id 由 4 位定长（`Q-0001`，上限 9999）升级为 **5 位定长（`Q-00001`，上限 99999）**——按 2025 年物理 20 卷 282 题的增速 + 待录的 2021-2024 真题 + 其他学科扩展，4 位不够用。全部 id 重写为 `Q-00001`~`Q-00322`；两份连可欣练习（检测 13 处 + 巩固 9 处）引用同步更新；validate 脚本新增 5 位格式校验；SKILL.md 规则注明"超过 95000 时评估再扩位"
- **改动原因**：用户指出 4 位编号上限 9999 题，题库扩容后不够
- **注意**：挂起前生成的旧 PDF（错题考点检测）中 id 仍为 4 位格式，内容本身无变化；如需一致可按需重新生成或忽略

### 2026-09-05
- **改动位置**：`question-bank/_questions.json` 数据修复（99 处核素符号）
- **改动内容**：核素符号 LaTeX 格式规范化——OMML 提取产物 `$ _{6}^{14}$C`（上下标独立成公式段、元素符号在公式外、下标在前）渲染错乱（显示成"614C"顺序混乱），统一修复为标准形式 `$^{14}_{6}\mathrm{C}$`（上标=质量数在前、下标=质子数、元素符号入公式内）。涉及浙江1月/安徽/福建/云南/湖北/黑吉辽蒙/浙江6月等卷。修复后全库体检 0 错误
- **改动原因**：用户发现练习中核素符号显示为"614^C→714^N"格式错乱；根因是 omml2latex 提取核素（Word 的 sPre 前置上下标结构）时拆成了独立公式段且下标顺序在前。**避坑规则：录入含核素/同位素符号的试卷后，必须检查核素格式是否为 `$^{质量数}_{质子数}\mathrm{元素}$`**

### 2026-09-05
- **改动位置**：`extract_docx.py`（选项处理增强）、`validate_question_bank.py`（规则9放宽）；批量录入 18 份 2025 物理高考卷 282 题（Q-0041~Q-0322，全库现 322 题/21 份试卷）
- **改动内容**：① `extract_docx.py` 选项逻辑三项增强：一行多选项自动拆分（`(?=[A-D][.．$])`）、「答案」段后停止收集选项（防解析续行"B．xxx错误"误收）、支持选项字母后直接跟 LaTeX 公式（`C$.` 格式）；② 体检规则放宽：解答题无图仅提示不告警（纯文字计算题原卷无图正常），实验题无图仍告警；③ 批量录入踩坑记录：**正则 `<(?:w|m):t[^>]*>` 会把 `<w:tab .../>` 误匹配为文本标签**（`<w:t`+`ab`），导致段落文本全是 XML 属性垃圾、题号匹配 0、图片归属全失败——正确写法 `<(?:w|m):t(?:\s[^>]*)?>`（此前河南卷提取时每行开头的垃圾前缀即此 bug，当时未深究）；江西卷题号用全角句号 `1．`（已兼容）；浙江 6 月卷第 14 题有两个子题（14Ⅰ/14Ⅱ 拆分）；福建 13 题暂缺、北京 20 题/陕晋宁青 6 题原卷标注"不选用"（跳过）
- **改动原因**：用户要求批量录入 2025 物理高考题目录中未录入的 18 份试卷；批量处理暴露了单卷录入时未显现的格式多样性问题，全部修复后全库体检 0 错误 0 警告

### 2026-09-05
- **改动位置**：新增 `scripts/validate_question_bank.py`；`SKILL.md` 第 3.5 步新增第 6 条「录入后必须跑体检」；修复题库存量缺陷（广东卷 Q-0014/Q-0019 选项两两挤行拆分为 4 条、Q-0017 图像选项题补占位选项；顺德卷 Q-0009/Q-0010 从原扫描 PDF 补裁配图存 `question-bank/images/顺德一中第四届物理运算（平行班）/`）
- **改动内容**：① 题库结构性体检脚本沉淀为 skill 工具，9 大项自动校验（id 连续/必填字段/枚举/选项答案匹配/去重/图片引用/试卷清单/配图覆盖），SKILL.md 规定每次录入后必跑；② 修复本次体检发现的 5 处存量缺陷，体检结果 0 错误 0 警告；③ 明确「结构校验 ≠ 语义校验」，答案/解析正确性仍需人工核对
- **改动原因**：用户问「题目录入是否完全没问题」，为给出可靠答复做全面体检，发现广东卷录入时选项拆分有缺陷（两选项挤一行）、顺德卷实验/解答题漏存配图；这类问题每次人工检查容易漏，固化为自动校验脚本

### 2026-09-05
- **改动位置**：`cleanup_tmp.py` 从 `scripts/` 迁移到工作区级 `.codebuddy/scripts/`；`SKILL.md` 第四步引用路径同步更新
- **改动内容**：临时文件清理脚本升级为**工作区级通用工具**（所有 skill 共用，docstring 与 `--help` 同步通用化），本 skill 及其余 3 个 skill（classroom-audio-evaluation / student-error-tracker / teaching-plan-planner）的 SKILL.md 统一引用 `.codebuddy/scripts/cleanup_tmp.py`
- **改动原因**：用户要求所有 skill 的临时文件统一放一个目录（`_tmp/`）不散落根目录，清理工具也相应从单个 skill 私有提升为工作区共享

### 2026-09-05
- **改动位置**：新增 `scripts/cleanup_tmp.py`；更新 `SKILL.md` 第四步（临时文件一键清理 + 复用已有脚本规范）；新建工作区根目录 `.gitignore`（排除 `_tmp/`、`*_work/`、`__pycache__/` 等）
- **改动内容**：① 规定所有一次性脚本/中间产物统一写入工作区 `_tmp/` 目录，结束时用 `scripts/cleanup_tmp.py` 一条命令清理，替代逐个文件删除（避免多次请求用户批准）；② SKILL.md 明确运行时优先复用 `scripts/` 已有工具（`extract_docx.py` 等），不要临场重写正则提取脚本
- **改动原因**：用户反馈每次运行后清理临时文件都要手动逐个确认，很麻烦；且本次河南卷录入时未发现已沉淀的 `extract_docx.py`（含完整 OMML→LaTeX 还原），重复手写了正则提取脚本，产生大量临时文件

### 2026-09-05
- **改动位置**：`SKILL.md` 第 3.5 步（docx 解析避坑）、题库录入实践
- **改动内容**：录入 2025 高考河南卷物理 15 题（Q-0026~Q-0040）+ 24 张图。沉淀三条避坑规则：① Windows PowerShell 命令行传中文路径/参数会 GBK 乱码，涉及中文路径必须写 UTF-8 编码的 .py 脚本文件执行，不能 `python -c` 内联；② docx 提取正文必须同时匹配 `<w:t>` 和 `<m:t>`（OMML 公式文本），否则分数/根号/上下标全部丢失；③ OMML 公式线性化后数值需人工还原核对——分数上下拼成连串数字（如 `114`→1/14、`34`→3/4）、分数线丢失（`sin45°sinθ`→sin45°/sinθ）、根号丢失（`2`→√2），务必结合答案数值反推验证后再写入题库
- **改动原因**：本次提取首次踩中 OMML 公式丢失与命令行中文乱码两个坑，公式线性化陷阱靠人工核对答案数值（如 T=T0/28≈13天、x/y=1/2）才还原正确，必须固化为规则避免下次出错

### 2026-09-05
- **改动位置**：更新 `SKILL.md`（第 3.5 步补充「插图保存」规范）
- **改动内容**：题目插图必须完整保存并关联——图片存 `question-bank/images/{试卷名}/`，Word 从 `word/media/` 提取并解析 `rId → media` 映射定位归属题号，题目记录新增 `images` 字段（数组存相对路径）
- **改动原因**：用户明确插图后续出题需要用到，必须完整保存；广东卷已提取 23 张图片并按题目归类关联

### 2026-09-05
- **改动位置**：更新 `SKILL.md`（新增「第 3.5 步：题目入库」）
- **改动内容**：分析试卷后，把每道题结构化写入统一题库 `question-bank/_questions.json`（字段：id/subject/grade/source_paper/question_no/question_text/options/answer/analysis/knowledge_point/exam_point/difficulty/question_type），并同步写入 `_papers.json` 试卷清单、更新 `_index.md` 题库概览；id 规则 `Q-` + 4 位全局递增序号；按 `source_paper + question_no` 去重
- **改动原因**：用户要批量导入试卷建立题库，供检索、组卷、错题出题复用；与 `student-error-tracker` 出题流程打通

### 2026-09-05
- **改动位置**：更新 `SKILL.md`（「公式书写规范」补充 Excel 专用说明），重写 `exam-paper-output\物理\高三_顺德一中第四届物理运算_题目分析.xlsx` 的公式单元格
- **改动内容**：Excel 版本公式改为**富文本真上下标**——用 openpyxl `CellRichText` + `InlineFont(vertAlign="subscript"/"superscript")` 渲染，把原先的线性写法 `E4-E2`、`φA>φB`、`B²L²v0`、`t1` 等全部改成真正的下标/上标（如 E₄-E₂、φ_A>φ_B、B²L²v₀、t₁），共 49 个下标 + 21 个上标；并沉淀「Excel 上下标避坑」规则：标记符 `~` 不能与区间符号 `~` 冲突（区间改用全角 `～`），分数保留线性写法但上下标用富文本
- **改动原因**：用户反馈 Excel 版本公式下标同样不规范；Excel 不支持 LaTeX 渲染，需用富文本 vertAlign 做真上下标，与 Markdown 的 LaTeX 方案互补

### 2026-09-05
- **改动位置**：更新 `SKILL.md`（新增「公式书写规范」），重写 `exam-paper-output\物理\高三_顺德一中第四届物理运算_题目分析.md` 中所有公式
- **改动内容**：新增「公式书写规范」章节——公式一律用 LaTeX 语法（`$...$`/`$$...$$`、`_{}`/`^{}`、`\frac{}{}`、希腊字母、`\mathrm{}` 单位），禁用 Unicode 上下标字符（`₁₂₀ₐₚ²` 等）与 `_A`/`_b` 式伪下标；同步把现有物理报告中的 `t₁`、`v₀`、`Eₚ`、`φ_A`、`B²L²` 等全部改写为 `$t_1$`、`$v_0$`、`$E_p$`、`$\varphi_A$`、`$B^2L^2$`
- **改动原因**：用户反馈报告公式下标不规范——Unicode 下标字形不完整、部分字体缺字形渲染成方块或错位，且混用 `_A` 式伪下标格式混乱；改用 LaTeX 后上下标规范美观，与听评课 skill 的公式导出规范一致

### 2026-09-05
- **改动位置**：新增 `CHANGELOG.md`，更新 `SKILL.md`（新增「自我总结与 Skill 优化」步骤）
- **改动内容**：建立「Skill 变更记录」机制；新增每次运行后的自我复盘与优化步骤——复盘踩坑（编码/路径/格式/脚本 bug）、把「下次还会遇到」的问题固化为规则、清理临时文件、追加变更记录
- **改动原因**：参考 `classroom-audio-evaluation` skill 的成熟做法，便于追踪本 Skill 演进，避免后期优化时破坏主体结构；把一次性试错变成永久规则
